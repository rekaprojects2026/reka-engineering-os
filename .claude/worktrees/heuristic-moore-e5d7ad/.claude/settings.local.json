{
  "permissions": {
    "allow": [
      "Bash(mkdir -p v0_extracted)",
      "Bash(powershell -Command \"Expand-Archive -Path 'Update Ui_dari v0_4.zip' -DestinationPath './v0_extracted' -Force\")",
      "Bash(npm install *)"
    ]
  }
}
